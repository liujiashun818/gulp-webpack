import React from 'react'
import ReactDOM from 'react-dom';
import { Router, Route, Link, IndexRoute} from 'react-router';
		
		const Equipment = React.createClass({
			render:function(){
				return(
					<div className='home'>Equipment</div>
					)
			}
		})
export default Equipment	