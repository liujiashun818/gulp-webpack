import React from 'react'
import ReactDOM from 'react-dom';
import { Router, Route, Link, IndexRoute} from 'react-router';
import './index.css';	
		
const NetWork = React.createClass({
			render:function(){
				return(
					<div className='home'>我是NotWork</div>
					)
			}
		})
export default NetWork	