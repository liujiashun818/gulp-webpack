import React from 'react';
import ReactDOM from 'react-dom';
import { Router, Route, Link, IndexRoute} from 'react-router';

var WatchManger = React.createClass({
  render: function() {
    return (
    		<div className = 'WatchManger'>
				我是  WatchManger
		    </div>				
  )}
});
export default WatchManger